---
import { Image } from 'astro:assets';
import logo from '../assets/logo-aurien-salud.webp';

interface Props {
    class?: string;
}

const { class: className } = Astro.props;
---

<Image src={logo} alt="Aurien Salud" width={160} height={46} class:list={['w-auto h-10', className]} />
